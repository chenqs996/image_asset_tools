# Week6 联调与验收报告（V1）

- 日期：2026-06-28
- 版本：V1
- 平台：Linux

## 1. 本周目标

- 端到端联调（导入→处理→导出）
- 回归测试与稳定性修复
- 打包构建与发布前检查
- 交付文档补齐

## 2. 执行结果

### 2.1 联调覆盖

- 导入：PNG/JPG/WebP/BMP 可导入并显示素材列表。
- 处理：
  - 切分三模式可切换并有预览反馈；
  - 缩放仅缩小规则生效；
  - 抠图三档算法可跑通，支持单图/批量/单张 override；
  - 帧动画支持播放、FPS、删帧、拖拽排序。
- 导出：支持批量选择、命名规则、格式统一输出（PNG/BMP/WebP）。

### 2.2 插件与架构

- ProviderRegistry 已接入运行时清单扫描与动态加载。
- 示例插件可被注册并显示于可用算法列表。

### 2.3 自动化校验

已执行：`npm run verify`

- lint：通过
- test：通过（3 files / 7 tests）
- build：通过（生成 `dist/`）

## 3. 性能与产物观察

- 构建正常完成，包含 ONNX Runtime wasm/js 资源。
- 当前构建体积中 ONNX 资源占比较高，属预期现象。
- 建议后续版本在 Linux 打包时增加模型资源按需分发策略。

## 4. 遗留与建议

1. 当前仅前端侧批量下载导出；后续可接 Tauri 原生文件系统以增强目录写入能力。
2. AI 抠图默认模型路径需配套真实模型文件。
3. Windows 发布链路仍未纳入本阶段（符合 V1 范围定义）。

## 5. 验收结论

V1 已达到发布候选（Release Candidate）条件：

- 核心功能闭环可用
- 架构扩展点（插件/Provider）可用
- 自动化验证通过
- 文档交付完整
